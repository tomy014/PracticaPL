#include <stdio.h>
 int main() {
int Modulo(int x, y) {
Modulo = x % y;
}
a = 10;
b = 3;
suma = Sumar(a, b);
resta = Restar(a, b);
producto = Multiplicar(a, b);
division = Dividir(a, b);
modulo = Modulo(a, b);

return 0;
}
